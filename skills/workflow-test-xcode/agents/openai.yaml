interface:
  display_name: "Workflow Test Xcode"
  short_description: "在当前 workflow/Codex 环境中执行 iOS 模拟器构建、冒烟测试与证据采集"
  default_prompt: "Use $workflow-test-xcode when iOS changes need simulator-based confidence in workflow; prefer local xcodebuildmcp, capture screenshots and logs, and fall back to native Xcode tools only when necessary."
